# Migration `20200513182006`

This migration has been generated by loan-petit at 5/13/2020, 6:20:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."Event.name"

ALTER TABLE "public"."Event" DROP COLUMN "cost",
ADD COLUMN "price" Decimal(65,30)   ,
ALTER COLUMN "description" DROP NOT NULL,
ALTER COLUMN "generateClientSheet" SET DEFAULT false;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200512172335..20200513182006
--- datamodel.dml
+++ datamodel.dml
@@ -1,29 +1,29 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRES_URL")
   default  = true
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id        Int      @default(autoincrement()) @id
-  email     String   @unique
+  id        Int     @default(autoincrement()) @id
+  email     String  @unique
   firstName String
   lastName  String
   password  String
   events    Event[]
 }
 model Event {
-  id                  Int      @default(autoincrement()) @id
-  name                String   @unique
-  description         String
+  id                  Int     @default(autoincrement()) @id
+  name                String
+  description         String?
   duration            Int
-  cost                Float
-  generateClientSheet Boolean
-  owner               User?    @relation(fields: [ownerId], references: [id])
+  price               Float?
+  generateClientSheet Boolean @default(false)
+  owner               User?   @relation(fields: [ownerId], references: [id])
   ownerId             Int?
 }
```


