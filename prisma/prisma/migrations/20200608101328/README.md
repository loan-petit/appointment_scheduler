# Migration `20200608101328`

This migration has been generated by loan-petit at 6/8/2020, 10:13:28 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."OAuthToken" (
"accessToken" text  NOT NULL ,"id" SERIAL,"refreshToken" text   ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."User" ADD COLUMN "googleId" text   ,
ALTER COLUMN "password" DROP NOT NULL;

CREATE UNIQUE INDEX "OAuthToken_userId" ON "public"."OAuthToken"("userId")

CREATE UNIQUE INDEX "User.googleId" ON "public"."User"("googleId")

ALTER TABLE "public"."OAuthToken" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200606164620..20200608101328
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRES_URL")
   default  = true
 }
 generator client {
@@ -11,20 +11,30 @@
 model User {
   id                      Int                     @default(autoincrement()) @id
   email                   String                  @unique
   username                String                  @unique
+  googleId                String?                 @unique
   firstName               String
   lastName                String
-  password                String
+  password                String?
   websiteUrl              String?
   phone                   String?
   address                 String?
   minScheduleNotice       Int                     @default(0)
+  oAuthToken              OAuthToken?
   appointmentTypes        AppointmentType[]
   recurrentAvailabilities RecurrentAvailability[]
   availabilityModifiers   AvailabilityModifier[]
 }
+model OAuthToken {
+  id           Int     @default(autoincrement()) @id
+  accessToken  String
+  refreshToken String?
+  user         User    @relation(fields: [userId], references: [id])
+  userId       Int
+}
+
 model AppointmentType {
   id                  Int     @default(autoincrement()) @id
   name                String
   description         String?
```


