# Migration `20200524182134`

This migration has been generated by loan-petit at 5/24/2020, 6:21:34 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Availability" (
"date" timestamp(3)  NOT NULL ,"endTime" integer  NOT NULL ,"id" SERIAL,"startTime" integer  NOT NULL ,"userId" integer   ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."AvailabilityModifier" DROP CONSTRAINT IF EXiSTS "AvailabilityModifier_userId_fkey";

ALTER TABLE "public"."Availability" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

DROP TABLE "public"."AvailabilityModifier";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200524140702..20200524182134
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRES_URL")
   default  = true
 }
 generator client {
@@ -18,9 +18,9 @@
   address                 String?
   minScheduleNotice       Int                     @default(0)
   events                  Event[]
   recurrentAvailabilities RecurrentAvailability[]
-  availabilityModifiers   AvailabilityModifier[]
+  availabilities          Availability[]
 }
 model Event {
   id                  Int     @default(autoincrement()) @id
@@ -41,16 +41,15 @@
   user      User? @relation(fields: [userId], references: [id])
   userId    Int?
 }
-model AvailabilityModifier {
-  id              Int      @default(autoincrement()) @id
-  date            DateTime
-  startTime       Int
-  endTime         Int
-  isInclusiveSlot Boolean
-  user            User?    @relation(fields: [userId], references: [id])
-  userId          Int?
+model Availability {
+  id        Int      @default(autoincrement()) @id
+  date      DateTime
+  startTime Int
+  endTime   Int
+  user      User?    @relation(fields: [userId], references: [id])
+  userId    Int?
 }
 enum Day {
   MONDAY
```


